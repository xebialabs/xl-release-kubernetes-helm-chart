apiVersion: v1
kind: Secret
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- include "render.value-if-not-secret" (dict "key" "releasePassword" "value" (include "release.secretPassword" .) ) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "releaseLicense" "value" (.Values.license | trim) ) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "repositoryKeystore" "value" .Values.keystore.keystore) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "keystorePassphrase" "value" .Values.keystore.passphrase) | nindent 2 -}}
  {{- if or .Values.external.db.enabled .Values.postgresql.install }}
  {{- include "render.value-if-not-secret" (dict "key" "mainDatabaseUsername" "value" (include "release.mainUsername" .)) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "mainDatabasePassword" "value" (include "release.mainPassword" .)) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "reportDatabaseUsername" "value" (include "release.reportUsername" .)) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "reportDatabasePassword" "value" (include "release.reportPassword" .)) | nindent 2 -}}
  {{- end -}}
  {{- if or .Values.external.mq.enabled .Values.rabbitmq.install }}
  {{- include "render.value-if-not-secret" (dict "key" "rabbitmqUsername" "value" (include "release.mqUsername" .)) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "rabbitmqPassword" "value" (include "release.mqPassword" .)) | nindent 2 -}}
  {{- end -}}
  {{- if .Values.oidc.external -}}
  {{- include "render.value-if-not-secret" (dict "key" "oidcClientId" "value" .Values.oidc.clientId) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "oidcClientSecret" "value" .Values.oidc.clientSecret) | nindent 2 -}}
  {{- if .Values.oidc.clientAuthJwt.enable -}}
  {{- include "render.value-if-not-secret" (dict "key" "oidcClientAuthJwtKeyStorePassword" "value" .Values.oidc.clientAuthJwt.keyStore.password) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "oidcClientAuthJwtKeyPassword" "value" .Values.oidc.clientAuthJwt.key.password) | nindent 2 -}}
  {{- end -}}
  {{- if .Values.oidc.accessToken.enable -}}
  {{- include "render.value-if-not-secret" (dict "key" "oidcAccessTokenSecretKey" "value" .Values.oidc.accessToken.secretKey) | nindent 2 -}}
  {{- end -}}
  {{- end -}}
  {{- if .Values.ssl.enabled -}}
  {{- include "render.value-if-not-secret-decode" (dict "key" "ssl_keystore.p12" "value" .Values.ssl.keystore) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "sslKeystorePassword" "value" .Values.ssl.keystorePassword) | nindent 2 -}}
  {{- include "render.value-if-not-secret" (dict "key" "sslKeystoreKeypassword" "value" .Values.ssl.keystoreKeypassword) | nindent 2 -}}
  {{- end -}}

{{- range $key, $value := .Values.extraSecrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ ternary (printf "%s-%s" (include "common.names.namespace" $) $key) $key $.Values.extraSecretsPrependReleaseName }}
  namespace: {{ include "common.names.namespace" $ | quote }}
  labels: {{- include "common.labels.standard" $ | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if $.Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $.Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
stringData: {{- include "common.tplvalues.render" (dict "value" $value "context" $) | nindent 2 }}
{{- end }}
